#!/usr/bin/env bash
BASE=$(realpath $(dirname $0)/../src)
grep -q $BASE ~/.bashrc || echo "source $BASE/.bashrc" >> ~/.bashrc
for f in $BASE/.{gitconfig,inputrc,tmux.conf,vimrc}; do ln -fsv $f ~; done
